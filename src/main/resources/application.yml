server:
  port: 8088

logging:
  level:
    org.springframework: debug

spring:
  message:
    basename: messages

management:
  endpoints:
    web:
      exposure:
        include: "*"

security:
  user:
    name : username
    password: passw0rd